{
  "version": 2,
  "installCommand": "pnpm install --prefer-offline --no-scripts",
  "buildCommand": "pnpm run db:generate && bash vercel-build.sh",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "regions": ["sfo1"]
}
